buildscript  {
    repositories {
        mavenCentral()
    }
}

allprojects {

    version = 'BINKS-1.0.0'
    group = 'fr.owle'

    repositories {
        mavenCentral()
        jcenter()
    }

}

project(':hometracker') {
    apply plugin: 'java-library'
}

project(':hello') {
    apply plugin: 'java-library'
}

task clean {
    subprojects.forEach { project ->
        finalizedBy ":$project.name:clean"
    }
}
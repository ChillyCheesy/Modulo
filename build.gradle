allprojects {
    version = hometracker_version as Object
    group = 'com.owle'
    repositories {
        mavenCentral()
    }
}

task clean {
    subprojects.forEach { project ->
        finalizedBy ":$project.name:clean"
    }
}
